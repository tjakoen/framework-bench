{
  "name": "framework-bench-astro",
  "version": "0.1.0",
  "description": "The Astro target for the framework bench: static-rendered HTML + one Astro <script> island (bundled, no UI-framework runtime) for the filter.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^26.1.1",
    "typescript": "^5.8.0"
  }
}
