{
  "name": "framework-bench-native",
  "version": "0.1.0",
  "description": "The native/BATCH target for the framework bench: server-rendered HTML + one client-safe filter module, no build step.",
  "type": "module",
  "private": true,
  "dependencies": {
    "@tjakoen/batch": "^0.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "bun-types": "^1.3.14",
    "typescript": "^5.8.0"
  },
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun server.ts",
    "export": "bun tools/export.ts",
    "check": "tsc --noEmit"
  }
}
